# Known round-trip failures in the test corpus.
# Each entry has: file (path relative to workspace root), reason, issue (GitHub issue number or 0).
# When an issue is fixed, remove its entry — the test will detect unexpected success.
